version: '3.9'

services:
  feadmin:
    container_name: feadmin
    hostname: admin.tailieutoan.vn
    image: feadmin
    build: .
    ports:
      - "5000:5000"
    networks:
      - docker_network

networks:
  docker_network:
    external: true